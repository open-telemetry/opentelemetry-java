receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:5678
  jaeger:
    protocols:
      thrift_http:

extensions:
  health_check:

exporters:
  logging:
    loglevel: debug

service:
  extensions: [health_check]
  pipelines:
    traces:
      receivers: [otlp, jaeger]
      exporters: [logging]