plugins {
    id 'java'
}

description = 'OpenTelemetry Examples for metrics'
ext.moduleName = "io.opentelemetry.examples.metrics"

dependencies {
    compile("io.opentelemetry:opentelemetry-api")

    //alpha modules
    compile("io.opentelemetry:opentelemetry-api-metrics")
}
