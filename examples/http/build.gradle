plugins {
    id 'java'
}

group 'io.opentelemetry'
version '0.3.0'

//create a single Jar with all dependencies
task fatJar(type: Jar) {
    baseName = project.name + '-all'
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
    with jar
}

repositories {
    mavenCentral()
}

dependencies {
    compile("io.opentelemetry:opentelemetry-api:${version}")
    compile("io.opentelemetry:opentelemetry-sdk:${version}")
    compile("io.opentelemetry:opentelemetry-exporters-logging:${version}")
    compile("io.grpc:grpc-protobuf:1.28.0")
    compile("io.grpc:grpc-netty-shaded:1.28.0")
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
